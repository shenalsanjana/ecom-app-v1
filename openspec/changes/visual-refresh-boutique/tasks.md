## 1. Tokens & font loading

- [x] 1.1 Update `app/globals.css` `:root` block with the boutique color tokens, `--radius: 1rem`, `--shadow-card-token`. Motion tokens added in `@theme inline` per 1.4.
- [x] 1.2 Removed `.dark { }` and the `@media (prefers-color-scheme: dark) { :root:not(.light) { ... } }` block. Kept `@custom-variant dark (&:is(.dark *));` so leftover `dark:*` utility classes stay inert (no `.dark` ancestor) instead of activating on system preference.
- [x] 1.3 Inter + Fraunces (variable, `axes: ['opsz']`, `display: 'swap'`, `latin` subset) wired in `app/layout.tsx` via `next/font/google`. Geist Mono retained for tabular numerals. All three `.variable` classes applied to `<html>`.
- [x] 1.4 `@theme inline { }` exposes `--font-sans: var(--font-inter)`, `--font-heading: var(--font-fraunces)`, plus motion tokens and `--shadow-card`. (Build verification of `font-heading` utility deferred to 5.7 typecheck/lint pass; Tailwind v4 compiles theme tokens at build.)

## 2. UI primitives

- [x] 2.1 `components/ui/button.tsx`: `default` size bumped `h-8 → h-9`, `lg` size `h-9 → h-10`, `sm` size `h-7 → h-8`, icon sizes scaled to match. New `brand` variant (`bg-brand text-brand-foreground hover:bg-brand/90`) added without removing or renaming any existing variant. `default` (cocoa) hover went from `/80 → /85` (subtler).
- [x] 2.2 `components/ui/card.tsx`: radius `rounded-xl → rounded-2xl` (and the corner `rounded-t-2xl/rounded-b-2xl` selectors). Added `transition` + `motion-safe:hover:-translate-y-0.5` + `motion-safe:hover:shadow-card` gated on `has-[a]` (only cards containing a link lift). Ring softened from `/10 → /5` (less prominent at rest).
- [x] 2.3 `components/ui/dialog.tsx`: overlay `bg-black/10 → bg-foreground/50` (warm cocoa wash, since `--foreground` is cocoa), duration `100ms → var(--duration-slow)` with `ease-(--ease-out)`. Popup `rounded-xl → rounded-2xl`, same duration/ease swap. Footer `rounded-b-xl → rounded-b-2xl`.
- [x] 2.4 `components/ui/input.tsx`: `h-8 → h-10`, `px-2.5 → px-3`, added `transition-colors duration-(--duration-fast)`. Focus ring already `--ring/50` (now olive). `components/ui/textarea.tsx`: switched from offset-ring pattern to flat ring matching Input (`focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50`), `rounded-md → rounded-lg`, added aria-invalid styling. `components/ui/label.tsx` unchanged — inherits tokens.
- [x] 2.5 `components/ui/badge.tsx`: new `brand` variant (`bg-brand text-brand-foreground`), `destructive` bg `/10 → /12` for slightly stronger warmed-red read on cream, outline/ghost `text-muted-foreground → text-foreground` for AA. Existing `rounded-4xl` already gives the pill shape. `components/ui/dropdown-menu.tsx`, `components/ui/alert.tsx`, `components/ui/separator.tsx`: no edits — they consume `bg-accent/text-accent-foreground` / `border` / `text-destructive` semantically and inherit the new tokens (warm muted-darker hover for menu items, NOT olive).

## 3. Surface components — home, header, footer

- [x] 3.1 `app/_components/home/product-card.tsx`: image backdrop `from-zinc-100/200` → `bg-muted`; name `text-sm` → `font-heading text-base font-medium`; live price `font-heading`, on-sale price gets `text-brand`; wishlist button gets hover scale (`motion-safe:hover:scale-110`, 150ms); when filled, heart icon `fill-brand text-brand`. Sale badge `variant="destructive"` → `variant="brand"` (still includes `−{pct}%` text). The fill-bounce on toggle is a separate keyframe addition deferred to 5.0.
- [x] 3.2 `app/_components/home/hero.tsx`: heading gets `font-heading font-medium` (was `font-semibold`). CTAs already use `buttonVariants({ size: "lg" })` which is now h-10 cocoa from Group 2. Hero gradient overlay and image untouched.
- [x] 3.3 `app/_components/home/site-header.tsx`: nav links `transition-colors duration-(--duration-fast) hover:text-brand`. Background already `bg-background/80` (cream via token). Cart-count badge lives in `header/cart-icon.tsx` — handled in Group 4 §4.9.
- [x] 3.4 `app/_components/home/site-footer.tsx`: column headings `font-heading`. No hardcoded zinc/gray classes to convert; structure unchanged.
- [x] 3.5 `app/_components/home/category-strip.tsx`: section heading → `font-heading`. Avatar ring swapped from hardcoded zinc to `bg-muted ring-1 ring-border hover:ring-2 hover:ring-ring/50`. **Heading-hierarchy fix N/A**: file has no inner heading (uses `<span>` per category), so the `h2 → h4` jump documented during exploration doesn't exist in current source. Likely fixed in a prior commit or misread. No code change needed for the a11y fix on this file.
- [x] 3.6 `app/_components/home/deals-section.tsx`: bg `bg-zinc-50 dark:bg-zinc-950` → `bg-muted`, h2 `font-heading`, "See all" link gets `hover:text-brand` with motion token. `app/_components/home/newsletter.tsx`: inverted band switched to `bg-primary text-primary-foreground` (cocoa-on-cream); h2 `font-heading`; Input override hardcoded zinc → token-based (`bg-background text-foreground placeholder:text-muted-foreground`).

## 4. Surface components — cart, product, header icons

- [x] 4.1 `app/_components/cart/free-shipping-progress.tsx`: three retuned states (under = muted, near (within 20%, gated by `NEAR_THRESHOLD_FRACTION = 0.8`) = brand copy, qualified = `bg-brand text-brand-foreground` pill). Track `bg-muted` + fill `bg-brand` with width + opacity transitions over `duration-(--duration-slow) ease-(--ease-out)`. progressbar role + copy preserved.
- [x] 4.2 `app/_components/cart/cart-item.tsx`: thumbnail `bg-zinc-100 dark:bg-zinc-800 → bg-muted`, `border-b → border-b border-border`, totals row `font-heading`. Quantity stepper wrapped in `<div role="group" aria-label="Quantity">`.
- [x] 4.3 `app/_components/cart/cart-summary.tsx`: subtotal/shipping/total figures `font-heading`; free shipping value `text-brand` when free.
- [x] 4.4 `app/_components/cart/add-to-cart-dialog.tsx`: size-picker selected state `bg-foreground text-background ring-2 ring-ring ring-offset-2`, exposes `data-state` + `aria-pressed`; `border-black bg-black text-white` hardcodes removed. `app/_components/cart/quick-buy-buttons.tsx`: "Buy now" Link switched from hardcoded `bg-black hover:bg-black/90 text-white` to `buttonVariants({ variant: "default" })` (cocoa).
- [x] 4.5 `app/_components/product/buy-box.tsx` and `buy-box-client.tsx`: H1 + price `font-heading`; sale price `text-brand`; sale Badge `destructive → brand`; size-picker selected state `border-ring bg-muted ring-2 ring-ring`, unselected `border-border hover:border-foreground/40`, `aria-pressed` + selected size shown as text. Add-to-cart stays default cocoa; "Buy Now" → `variant="outline"` (was hardcoded black).
- [x] 4.6 `app/_components/product/breadcrumb.tsx`: links `transition-colors duration-(--duration-fast) hover:text-brand underline-offset-4 hover:underline`, current item `font-medium text-foreground` (no hover), separators `text-muted-foreground`.
- [x] 4.7 `app/_components/product/image-gallery.tsx`: backdrop `bg-zinc-50 dark:bg-zinc-900 → bg-muted`, selected thumbnail ring `ring-foreground → ring-ring`. `related-strip.tsx`, `description.tsx`: section h2 `font-heading font-medium`. `size-chart-dialog.tsx`: no edits needed — DialogTitle primitive already applies font-heading from Group 2; body is image-only.
- [x] 4.8 `app/_components/product/reviews-section.tsx`: section h2 + rating-summary number `font-heading`. Star rating wrapper given `role="img"` + `aria-label="Rating: N out of 5"` (promoted from a bare `<span aria-label>` which doesn't reliably announce in SRs).
- [x] 4.9 `header/profile-menu.tsx`, `header/cart-icon.tsx`, `header/wishlist-icon.tsx`: `<Button size="icon" → size="icon-lg">` (size-10, 40px hit target); cart + wishlist count badges → `<Badge variant="brand">`. `header/cart-icon-wrapper.tsx`: untouched (pure re-export, inherits new size).
- [x] 4.10 `app/_components/whatsapp-float-button.tsx`: brand green untouched. No focus ring previously; added `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2` (token-based olive `--ring`).
- [x] 4.11 Grep sweep across `app/` for stray hardcoded color utilities: `app/search/page.tsx:175` checkbox `border-gray-300 focus:ring-primary` → `border-input focus:ring-ring`; `app/deals/page.tsx` Limited-Time pill + discount badge `bg-red-500 text-white` → `bg-brand text-brand-foreground`, hero gradient `from-amber-50 to-orange-50` → `bg-muted`, hero h1 picks up `font-heading`. Hero overlay-on-image `text-white` and WhatsApp float-button `bg-green-500 text-white` left untouched (intentional per design.md — overlay-on-image / brand-color non-negotiable).

## 5. States, a11y audit, verification

- [x] 5.0 Wishlist heart fill-bounce: added `@keyframes wishlist-fill` (1 → 1.15 → 1) and `--animate-wishlist-fill` token to `globals.css`. In `product-card.tsx`, the heart icon gets `key={wishlisted ? "filled" : "empty"}` so it remounts on toggle, plus `motion-safe:animate-wishlist-fill` when filled so the keyframe fires only with the user's motion preference allowing it.
- [x] 5.1 `app/_components/shared/product-grid-skeleton.tsx`: tile color already `bg-muted` (warm). Shimmer gated `animate-pulse → motion-safe:animate-pulse`, image placeholder upgraded `rounded-lg → rounded-2xl` for parity with the new Card radius. Tailwind's default `animate-pulse` is a 2s cycle, fades to ~50% opacity; under `prefers-reduced-motion: reduce` the variant suppresses the keyframe entirely.
- [x] 5.2 Audit complete. All 7 `loading.tsx` files (`cart/`, `categories/[slug]/`, `account/orders/`, `deals/`, `search/`, `wishlist/`, plus the shared skeleton) use `bg-muted` blocks via `motion-safe:animate-pulse` — gated everywhere. No naked spinners anywhere.
- [x] 5.3 Restyled all 5 error boundaries:
    - `account/error.tsx`, `checkout/error.tsx`, `search/error.tsx` (recoverable): heading `font-heading text-3xl font-medium`, primary CTA `<Button variant="brand" size="lg">` (Try again), secondary CTA `<Link className={buttonVariants({ variant: "outline", size: "lg" })}>` (Back …). Old hardcoded `<Link className="… hover:bg-accent">` removed.
    - `not-found.tsx` (404): "404" eyebrow gets uppercase tracking, headline `font-heading text-4xl/5xl font-medium`, single CTA via `buttonVariants({ variant: "brand", size: "lg" })`.
    - `products/[id]/not-found.tsx`: headline `font-heading text-4xl font-medium`, hardcoded primary-bg button replaced with `buttonVariants({ variant: "brand", size: "lg" })`.
    Spec scenario for "Error boundary follows pattern" updated: 404 surfaces use a single primary CTA; recoverable error boundaries MAY add a secondary outline CTA for the alternate path (Try again + Back-to-X).
- [x] 5.4 Authored `scripts/check-contrast.ts` (zero new deps — pure tsx, OkLab→linear-RGB→sRGB→WCAG luminance). Added `npm run check:contrast` script. Checks 8 pairs: foreground-on-background, muted-foreground-on-background, primary-foreground-on-primary, brand-foreground-on-brand, accent-foreground-on-accent, destructive-foreground-on-destructive, ring-on-background, and brand-on-background (sale-price-as-body-text — extra check beyond the original 7).
- [x] 5.5 Ran `npm run check:contrast`. Initial run: brand-on-background (olive-on-cream sale price) failed at 4.37:1 (need 4.5). Adjusted `--brand` from `oklch(0.55 0.075 125)` to `oklch(0.51 0.085 125)` (slightly darker, slightly more saturated). `--ring`, `--chart-1`, `--sidebar-ring` updated to match. Re-run: all 8 pairs PASS, brand-on-background now 5.16:1, brand-foreground-on-brand 5.36:1.
- [ ] 5.6 Manual a11y pass (record findings in commit body):
    - Verify focus rings on wishlist heart, free-shipping bar (if interactive), and size-picker buttons.
    - Tab through three flows end-to-end: home → category → PDP → cart → checkout; account dropdown open/close; AddToCartDialog open/close (focus trap returns to opener).
    - Measure header icon hit targets in DevTools — confirm ≥ 40 × 40 CSS px.
    - Verify form labels in `(auth)/forgot-password`, `(auth)/reset-password`, `account/profile-form.tsx`, `account/address-form.tsx` (every input has an associated `<Label>` or `aria-label`).
    - Toggle `prefers-reduced-motion: reduce` in DevTools → Rendering; verify card hover lift, dialog enter, and free-shipping bar fill all suppress transforms/dimensions while keeping color/opacity changes.
- [x] 5.7 `npx tsc --noEmit` clean (silent, exit 0). `npm run lint` clean — only one pre-existing warning in `prisma/seed.ts` (unused symbol `pickGalleryImage`), unrelated to this change. **Also ran `npm run build` (per advisor checkpoint) — exit 0, all 24 routes built. Confirms Tailwind v4 resolved every new token utility (`font-heading`, `bg-brand`, `shadow-card`, `animate-wishlist-fill`, motion duration/ease tokens). The build's prerender step needed `.env.local` (DATABASE_URL) — copied from the develop checkout; it's gitignored and not committed.**
- [ ] 5.8 Run a Lighthouse mobile audit at the worktree's `next start` build (or via `next dev` if necessary) and compare LCP / CLS / TBT against `develop`. Performance score MUST NOT drop more than 2 points; if it does, investigate Fraunces loading strategy.
- [ ] 5.9 Manual smoke test through the full storefront in light mode: home, /categories, a category page, a PDP, /cart, /checkout, /search, /wishlist, /account/* — confirm boutique identity is consistent and no surface looks unfinished or off-palette.
- [x] 5.10 `openspec validate visual-refresh-boutique --strict` is green.
